/** Cycling colours for dashboard map markers and SVG connector lines. */
export const MARKER_COLORS = ['#22d3ee', '#86efac', '#f472b6'] as const;

/** Red used for ping dots and trail lines on the Send Ping map. */
export const PING_COLOR = '#ef4444';

/** Maximum pings kept in a visible trail (client state + DB walk). */
export const TRAIL_LIMIT = 5;
